import React from 'react';
import * as yup from 'yup';
import { useFormik } from 'formik';

const LoginForm = () => {
  return (
    <form>
      Login form
    </form>
  );
}

export default LoginForm;