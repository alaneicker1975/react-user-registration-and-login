import React, { Fragment } from 'react';
import LoginForm from '../components/login-form';

const Login = () => {
  return (
    <Fragment>
      <h1>Login</h1>
      <LoginForm />
    </Fragment>
  );
};

export default Login;